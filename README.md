# config.json
EASY_CD:
Cooldown for commands that do not require a long time to be processed (e.g. no API calls).
Also acts as the default cooldown for commands if it is not specified.

# To setup the bot
In terminal,
```bash
node index.js
```