# About the webapp
This webapp is meant to supplement features of the bot, and is built using Flask.

## Features (WIP)
- Discord user login
- uploading and parsing of csv files
- easier input of data via a form interface
- server settings
- interface to access collections of data (e.g. for created challenges)